<!doctype html>
<html lang="sk">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="Ručná výroba a predaj krásnych čeleniek pre deti a mamky vyrobené z kvalitných materiálov zo Slovenska. Čelenky pre mamky, čelenky pre deti, čelenky pre bábätká. Čelenky z baby friendly materiálov.">
    <meta name="author" content="Attila Jakab">
    <link rel="icon" type="image/png" href="../assets/celenka-logo.png"/>
    <title>čelenka - ručná výroba a predaj čeleniek pre deti a mamky. Čelenky pre mamky, čelenky pre deti, čelenky pre bábätká. Čelenky z baby friendly materiálov.</title>
  
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css" preconnect>
    <link href="../main.css" rel="stylesheet">
    <script src="https://www.google.com/recaptcha/api.js?hl=sk" async defer></script>
  </head>
  <body>
    {{> header }}

    <main class="container-lg my-5">

      <section class="row">
        <div class="col-md-6">
          <div id="product-carousel" class="carousel slide" data-bs-interval="false">
            <div class="carousel-indicators">
              {{#each imgPath}}
                <button type="button" data-bs-target="#product-carousel" data-bs-slide-to="{{@index}}" {{#ifLt @index 1}} class="active" aria-current="true" {{/ifLt}} aria-label="Obrázok {{@index}}"></button>
              {{/each}}
            </div>
            <div class="carousel-inner">
              {{#each imgPath}}
                <div class="carousel-item {{#ifLt @index 1}} active {{/ifLt}}">
                  <img src="{{this}}" class="d-block w-100" alt="Fotka produktu čelenky">
                </div>
              {{/each}}
            </div>
            <button class="carousel-control-prev" type="button" data-bs-target="#product-carousel" data-bs-slide="prev">
              <span class="carousel-control-prev-icon" aria-hidden="true"></span>
              <span class="visually-hidden">Nasledujúce</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#product-carousel" data-bs-slide="next">
              <span class="carousel-control-next-icon" aria-hidden="true"></span>
              <span class="visually-hidden">Predchádzajúce</span>
            </button>
          </div>
        </div>
        <div class="col-md-6">
          <div>
            <h1 class="display-4">{{title}}</h1>
          </div>
          <p class="mt-4">Kategória: <span class="cbadge">{{category}}</span></p>
          <p class="mt-4 lead">{{description}}</p>
          <div class="mt-5 text-end">
            <hr>
            <p style="font-size: 20px;">{{price}}</p>
          </div>
        </div>
      </section>

      <!-- MODALS -->
      {{> privacyPolicyModalPartial }}
      {{> termsConditionsModalPartial }}
      {{> measurementModalPartial }}
    </main>

    <!-- Footer -->
    {{> footer }}

    {{!-- @TODO --}}
    {{!-- <script type="text/javascript" src="../assets/js/common.js"></script> --}}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
  </body>
</html>
